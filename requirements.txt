opencv-python>=4.8           # video capture and rendering
numpy>=1.24
onnxruntime>=1.18            # runs all models (vehicle, plate, colour)

# license plate reading
open-image-models[onnx]>=0.5   # plate localization
fast-plate-ocr>=1.0            # plate OCR
